#!groovy

node {

   stage('Build - Jenkins in Jenkins ') {
     sh 'echo Build - Jenkins in Jenkins '
   }
   
   stage('Run npm') {
     sh 'npm -v'
   }
   
   stage('Run Java') {
     sh 'java -version'
   }
   
   //stage('git clone) {
     //sh 'git clone https://github.com/ernesen/test.git'
     //sh 'ls -al ./test/'  
     //sh 'echo "I am trying to get this working here"' 
     //sh 'kustomize version'
   //}
}
