#!groovy

node {

   stage('Build - Jenkins in Jenkins ') {
     sh 'echo Build - Jenkins in Jenkins '
      sh "echo MY_PARAM=${MY_PARAM}" 
   }
   
   stage('Run npm') {
     sh 'npm -v'
   }
   
   stage('Run Java') {
     sh 'java -version'
   }
}
